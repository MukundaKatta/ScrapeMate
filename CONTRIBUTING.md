# Contributing to ScrapeMate

## Setup
```bash
git clone https://github.com/MukundaKatta/ScrapeMate.git
cd ScrapeMate && make dev
```

## Development
- Run tests: `make test`
- Lint: `make lint`
- Format: `make fmt`
